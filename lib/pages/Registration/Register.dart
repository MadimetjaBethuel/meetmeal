import 'package:flutter/material.dart';
import 'components/register_layout.dart';

class Register extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: RegisterLayout(),
    );
  }
}
